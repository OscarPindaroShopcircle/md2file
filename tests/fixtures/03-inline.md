# Inline formatting

This paragraph has **bold text**, *italic text*, and `inline code`.

Here is a [link to example](https://example.com) inside a sentence.

Nested inline: **bold with *italic inside* and `code` too**, plus a
[**bold link**](https://example.org).
